pixelscript = {
    name = "InvalidFor",
    modules = {"LED"},
    entrypoint = "main"
}

function main()
    for k, v in pairs({1, 2, 3}) do
        led.clear()
    end
end

=== OUTPUT ===
Error: Parse error
   ╭─[testprogs/test.pxs:2:7]
   │
 2 │     name = "InvalidFor",
   │       ┬  
   │       ╰── Expected 'In', 'Assign', but found Comma
───╯